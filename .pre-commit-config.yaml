repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.3.0
    hooks:
      - id: ruff
        args: [ --fix ]
      - id: ruff-format

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
      - id: check-json
      - id: debug-statements
      - id: check-ast
      - id: check-merge-conflict

  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.9.0
    hooks:
      - id: mypy
        name: mypy-core
        args: [--config-file=nexus_core/pyproject.toml, --explicit-package-bases]
        additional_dependencies: [
          pydantic>=2.13.4,
          types-requests,
          types-psutil,
          click>=8.1.0,
          rich>=13.0.0,
          discord.py>=2.3.0
        ]
        files: ^nexus_core/
      - id: mypy
        name: mypy-edge-scraper
        args: [--config-file=nexus_edge_scraper/pyproject.toml, --explicit-package-bases]
        additional_dependencies: [
          pydantic>=2.13.4,
          rich>=13.0.0,
          attrs>=23.1.0,
          fastapi,
          types-requests,
          types-psutil,
          types-beautifulsoup4,
          httpx,
        ]
        files: ^nexus_edge_scraper/

  - repo: https://github.com/returntocorp/semgrep
    rev: 'v1.165.0'
    hooks:
      - id: semgrep
        args: ['--config', auto, '--error']
        stages: [pre-push]

  - repo: local
    hooks:
      - id: core-test
        name: core-test
        entry: bash scripts/docker_test.sh
        language: system
        stages: [pre-push]
        always_run: false
        files: ^nexus_core/.*\.py$
        pass_filenames: false

      - id: scraper-test
        name: scraper-test
        entry: bash -c "PYTHONPATH=nexus_edge_scraper nexus_core/.venv/bin/pytest nexus_edge_scraper/tests"
        language: system
        stages: [pre-push]
        always_run: false
        files: ^nexus_edge_scraper/.*\.py$
        pass_filenames: false
