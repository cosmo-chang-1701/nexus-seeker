# ==========================================
# Nexus Seeker Edge Scraper - Env Variables
# ==========================================

# 1. Cloudflare Tunnel Token (必填)
# 用途：將 FastAPI 邊緣爬蟲安全地暴露到公網，供 Core Bot 呼叫。
# 取得方式：請至 Cloudflare Zero Trust Dashboard -> Access -> Tunnels 新增 Tunnel，並複製其 Token。
CF_TUNNEL_TOKEN=your_tunnel_token

# 2. Reddit User-Agent (強烈建議設定，避免抓取 Reddit RSS 時被限流/封鎖)
# Reddit 對未使用自訂且獨特 User-Agent 的連線會優先限流。請改用你自己的 Reddit 帳號。
# 格式: <platform>:<app_id>:<version> (by /u/<your_reddit_username>)
REDDIT_USER_AGENT=script:nexus-seeker-sentiment-tracker:v1.0 (by /u/your_reddit_username)

# 3. GEX 快照歷史 (選填，前向蒐集)
# gex_snapshot 只保留最新值，GEX 相關門檻因此無法回測。開啟後每個標的每 15 分鐘保留
# 一筆歷史快照 (履約價裁切至現價 ±25%、zlib 壓縮，約 0.8MB/日)，供 nexus_core 的
# 離線校準工具讀取。保留期預設 180 天。
GEX_HISTORY_ENABLED=true
GEX_HISTORY_RETENTION_DAYS=180
